'use strict';

describe('statScheduleApp.calendar module', function() {

  beforeEach(module('statScheduleApp.calendar'));

  describe('calendar controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var calendarCtrl = $controller('calendarCtrl');
      expect(calendarCtrl).toBeDefined();
    }));

  });
});